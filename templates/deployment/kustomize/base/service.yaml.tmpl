apiVersion: v1
kind: Service
metadata:
  name: "{{ .Service.Name.DNSCase }}"
  namespace: "{{ .Namespace }}"
spec:
  clusterIP: None
  selector:
    app: "{{ .Service.Name.DNSCase }}"
  ports:
    - protocol: TCP
      name: tcp-port
      port: 9000
      targetPort: 9000
