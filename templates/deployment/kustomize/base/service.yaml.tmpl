apiVersion: v1
kind: Service
metadata:
  name: "{{ .Service.Name.DNSCase }}"
  namespace: "{{ .Namespace }}"
spec:
  selector:
    app: "{{ .Service.Name.DNSCase }}"
  ports:
    - protocol: TCP
      name: tcp-port
      port: 9000
      targetPort: 9000
